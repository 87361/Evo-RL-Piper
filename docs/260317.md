# 260317 训练入口统一结论

## 结论

当前仓库已彻底移除 `training_repo` 线性 openpi smoke 训练链路，`openpi` 训练统一指向 `third_party/openpi` 官方训练入口。

## 已移除

- `scripts/train_openpi.py`
- `src/training_repo/backend_openpi/`
- `configs/train_openpi.yaml`
- `configs/train_openpi_A_smoke.yaml`
- `tests/training_repo/test_train_openpi_smoke.py`

## 当前唯一训练入口

- 启动脚本：`scripts/train_pi.py`
- 转发目标：
  - `openpi_jax` -> `third_party/openpi/scripts/train.py`
  - `openpi_torch` -> `third_party/openpi/scripts/train_pytorch.py`

## 推荐配置

- `configs/train_pi0_openpi.yaml`
- `configs/train_pi05_openpi.yaml`

## 使用示例

```bash
python scripts/train_pi.py --config configs/train_pi0_openpi.yaml
python scripts/train_pi.py --backend openpi_torch pi05_aloha --exp-name=my_run --overwrite
```

结论先说：当前仓库“整体上”还不支持在 training_repo 这层通过 YAML 切换到 act/diffusion；但 lerobot 子系统本身已经具备通过配置选择 policy（含 act、diffusion）的能力。
1) 已有能力
lerobot 训练链路已是“配置驱动 policy”：
/data/vepfs/users/intern/lingyue.yang/Evo-RL-Piper/src/lerobot/scripts/lerobot_train.py 在训练时直接用 cfg.policy 调 make_policy(...)。
TrainPipelineConfig 已声明 policy: PreTrainedConfig：
/data/vepfs/users/intern/lingyue.yang/Evo-RL-Piper/src/lerobot/configs/train.py，且 validate() 要求 policy 必填。
policy factory 已明确支持 act 和 diffusion：
/data/vepfs/users/intern/lingyue.yang/Evo-RL-Piper/src/lerobot/policies/factory.py 中：
get_policy_class() 分支含 name == "act" / name == "diffusion"
make_policy_config() 分支含 policy_type == "act" / policy_type == "diffusion"
make_pre_post_processors() 对 ACTConfig / DiffusionConfig 均有分支
PreTrainedConfig 子类注册也已存在：configuration_act.py 与 configuration_diffusion.py 都是 @PreTrainedConfig.register_subclass(...)。
代码证据（节选）：
lerobot_train.py
Lines 269-274
    if is_main_process:        logging.info("Creating policy")    policy = make_policy(        cfg=cfg.policy,        ds_meta=dataset.meta,
train.py
Lines 47-48
    policy: PreTrainedConfig | None = None
factory.py
Lines 82-87
    elif name == "diffusion":        from lerobot.policies.diffusion.modeling_diffusion import DiffusionPolicy...    elif name == "act":        from lerobot.policies.act.modeling_act import ACTPolicy

2) 让 training_repo 层支持“多策略 YAML”的最小改造清单与顺序
下面是我建议的最小改造（不动 lerobot 核心）：
第 1 步（先打通分发）
改 /data/vepfs/users/intern/lingyue.yang/Evo-RL-Piper/src/training_repo/train/orchestrator.py
引入路由规则：backend + policy.type（至少支持 act/diffusion）
保持现有 openpi 路径不变（兼容）
第 2 步（新增 lerobot 适配 backend）
新增 /data/vepfs/users/intern/lingyue.yang/Evo-RL-Piper/src/training_repo/backend_lerobot/backend.py
实现 TrainingBackend.train(config)
在这里调用 lerobot 训练入口（建议直接复用 lerobot_train.train 或等价调用），把 YAML 里的 policy.type: act|diffusion 传进去
第 3 步（统一脚本入口）
改 /data/vepfs/users/intern/lingyue.yang/Evo-RL-Piper/scripts/train_openpi.py（或新增 scripts/train.py）
文案从 “openpi only” 改成 “generic training config”
默认配置可仍指向 openpi，但允许 act/diffusion 的 YAML 直跑
第 4 步（补最小配置样例）
新增或补充 YAML（至少两份）：
/data/vepfs/users/intern/lingyue.yang/Evo-RL-Piper/configs/train_act.yaml
/data/vepfs/users/intern/lingyue.yang/Evo-RL-Piper/configs/train_diffusion.yaml
现有 openpi YAML 保留：train_openpi*.yaml

## 260317 训练入口统一结论（更新）

当前仓库已切换为“只保留真实 PI 训练入口”：

1. 已移除路径
- `scripts/train_openpi.py`（线性 smoke 训练入口）已移除。
- `src/training_repo/backend_openpi/`（线性后端）已移除。
- `configs/train_openpi*.yaml` 已移除。

2. 唯一推荐入口
- 新入口：`scripts/train_pi.py`
- 目标后端：
  - `openpi_jax` -> `third_party/openpi/scripts/train.py`
  - `openpi_torch` -> `third_party/openpi/scripts/train_pytorch.py`

3. 配置方式
- `configs/train_pi0_openpi.yaml`
- `configs/train_pi05_openpi.yaml`

4. 运行示例
```bash
python scripts/train_pi.py --config configs/train_pi0_openpi.yaml
python scripts/train_pi.py --backend openpi_torch pi05_aloha --exp-name=my_run --overwrite
```
