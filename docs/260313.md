# 训练仓库最小闭环模块契约表（按当前代码实现）

| 模块 | 输入 | 输出 | 不变量 | 失败时应做什么 |
| --- | --- | --- | --- | --- |
| ingest | `raw_data_root` 下原始 episode json | 标准化 episode 列表（`episode_id/task_id/source_path/num_steps/success/steps`） | step 按 `t` 升序；episode 和 step 必要字段齐全；`steps` 非空 | 立即失败并抛出明确异常：路径不存在（`FileNotFoundError`）、无 json/字段缺失/结构非法（`ValueError`，带文件路径与缺失键） |
| relabel | 标准化 episode + `pre_intervention_k` | 带标签 step（含 `sample_id/sample_type/label_source`） | `sample_id = {episode_id}:{t}`；`sample_type ∈ {correct, interaction, incorrect}`；每次 `intervention_flag` 从 `False->True` 的切换点都会触发 pre-intervention 回溯 K 步（不是仅第一次）并改成 `incorrect` | 对非法参数直接失败：`pre_intervention_k < 0` 抛 `ValueError`；其余逻辑异常应保留原始报错用于定位 |
| dataset_build | episodes + 构建配置（`val_ratio/split_mode/shard_size/...`） | OpenPI 可读数据包（`steps/`、`manifests/`、`labels/`、`meta/`） | 仅 `episode` 级 split；split 以 episode 为单位且同一 episode 的 step 不得跨 split；`sample_id` 全局唯一且格式固定；`bucket` 合法；`obs_state/action` 维度全局一致；统计只用 train split | 配置不合法或数据不一致时立刻中止：`val_ratio`/`split_mode`/`shard_size` 异常、重复 `sample_id`、维度不一致、train 为空均抛 `ValueError` |
| adapter | 数据包根目录（manifest/labels/steps/stats） | 按 split 的训练样本列表（含 step 字段 + 标签字段） | 以 `sample_id` 做跨文件对齐；`build_manifest.bucket` 与 label 的 `sample_type` 语义一致；样本字段最小集完整 | 文件缺失或 key 缺失时快速失败（`FileNotFoundError`/`KeyError`）；`sample_id` 跨文件对不齐时必须失败，不允许静默跳过或自动补齐；若 train split 空，后续 dataloader 会抛 `ValueError("No train samples found...")` |
| train | 训练配置（`backend=openpi`、数据根目录、采样比、超参） | 训练产物（`model.npz`、`metrics.json`、`train_config_snapshot.json`） | 不回写 dataset；只消费 `train` split 参与参数更新，不读取 `val` split 做更新；采样比总和 > 0；请求到的 bucket 必须在数据中存在；`obs_dim/action_dim` 与数组形状一致 | 后端不支持时报 `ValueError`；采样比非法或 bucket 空时报 `ValueError`；空训练样本时报 `ValueError` 并提示先 build dataset |
| export | 导出配置（`artifact_dir/artifact_weights_file/tos_weights_path`） | `inference_bundle`（`inference_runner.py/model_spec.yaml/weights_locator.yaml/...`） | bundle 文件集固定；`model_spec` 维度由权重推导；`load_policy()` 返回含 `weight/bias/obs_mean/obs_std/action_mean/action_std` 的 policy 字典；`predict_action()` 最小输入是 1D `obs_state` 向量；输出 `action` shape 为 `[action_dim]`，由 `model_spec` 与权重形状共同定义 | 权重文件缺失时报 `FileNotFoundError`；权重数组缺 key/shape 不匹配时报 `ValueError`；输入 `obs_state` 维度不符时报 `ValueError` |

## 系统级不变量（横跨模块）

- `sample_id` 在整个 dataset package 内全局唯一。
- dataset build 产物一旦生成，训练与导出流程都不得回写修改该数据包。
- batch ratio 只存在于训练侧 sampler，不存在于 dataset build 产物定义中。
- inference bundle 运行时不依赖 training repo 内部复杂路径，仅依赖 bundle 内文件与 `weights_locator.yaml` 的权重定位约定。

## 备注

- 本表基于代码实现梳理，主要对应：
  - `src/training_repo/ingest/loader.py`
  - `src/training_repo/relabel/apo.py`
  - `src/training_repo/dataset_build/builder.py`
  - `src/training_repo/backend_openpi/dataset_adapter.py`
  - `src/training_repo/backend_openpi/dataloader.py`
  - `src/training_repo/backend_openpi/sampler.py`
  - `src/training_repo/backend_openpi/backend.py`
  - `src/training_repo/train/orchestrator.py`
  - `src/training_repo/export/bundle.py`

复验结果：
python scripts/export_inference_bundle.py --config configs/export_inference.yaml
退出码 0，成功打印预期字段。
python -m pytest -q tests/training_repo/test_export_bundle.py
退出码 0，1 passed。
python scripts/smoke_telemanipulation_bundle.py --bundle-dir deploy/inference_bundle
退出码 0，输出 telemanipulation bundle smoke passed，checks 为 4 项（1 正向 + 3 错误场景）。
额外说明：
这次补的是验收用最小 mock 权重。如果要对接真实训练产物，只需要把 artifacts/openpi_v0/run_001/model.npz 替换成真实权重（或改 artifact_weights_file 指向真实文件）即可。


实现围绕 4 点收口：相对路径、episode 级 split、全局唯一 sample_id、OpenPI 最小可读契约显式化。

## 已完成改动

- **builder 稳健性收口**
  - 更新 `src/training_repo/dataset_build/builder.py`
  - 新增：
    - 配置路径解析：相对路径以配置文件目录为锚点解析
    - `split_mode` 仅支持 `episode`（默认）
    - `shard_size > 0` 校验
    - `sample_id` 格式强约束：`<episode_id>:<t>`
    - `sample_id` 全局唯一校验（build 全过程）
    - `obs_state/action` 维度一致性校验
- **配置对齐 v2**
  - 更新 `configs/dataset_build.yaml`
  - 显式加入 `split_mode: episode`
- **类型定义同步**
  - 更新 `src/training_repo/dataset_build/config.py`
  - `DatasetBuildConfig` 增加 `split_mode`
- **最小 fixture 数据**
  - 新增 `tests/fixtures/training_repo/raw_episodes/episode_0001.json`
  - 新增 `tests/fixtures/training_repo/raw_episodes/episode_0002.json`
- **E2E 最小闭环测试**
  - 新增 `tests/training_repo/test_dataset_build_min_loop.py`
  - 覆盖：
    - relabel 三类样本 + pre-intervention
    - episode 级 split
    - sample_id 全局唯一
    - OpenPI 最小契约字段
    - stats 维度一致性
    - adapter 可读 train/val
- **CLI smoke**
  - 新增 `scripts/smoke_dataset_openpi.py`
  - 校验数据包最小契约、跨文件 `sample_id` 对齐、`bucket==sample_type`、stats 维度、OpenPI adapter 可读性
- **契约文档显式化**
  - 新增 `docs/dataset-build-min-loop-v0.md`
  - 明确最小字段、路径规则、split/sample_id 规则与 smoke 命令
- **OpenPI 最小训练 smoke 闭环补齐**
  - 新增 `src/training_repo/backend_openpi/dataloader.py`
  - 说明：仅新增 `build_training_arrays(...)` 作为“数组准备函数收口”，不引入 DataLoader/Dataset/Collator 新抽象层
  - 修改 `src/training_repo/backend_openpi/backend.py`
    - 复用 `build_training_arrays(...)` 进行 `x_all/y_all/sample_types` 准备
    - 保留现有 `ProportionalBucketSampler` 与训练循环不变
    - 保留现有 artifact 落盘不变：`model.npz`、`metrics.json`、`train_config_snapshot.json`
  - 新增 `tests/training_repo/test_train_openpi_smoke.py`
    - 覆盖最小链路：`dataset package -> adapter -> sampler -> 1 epoch/1 step training -> artifact 落盘`
    - 验证 `metrics` 可读（`train_loss_history` 与 `final_train_loss`）
    - 通过训练前后文件哈希快照断言“训练不回写 dataset build 产物”
  - 采样职责边界保持不变：
    - APO 比例采样只在训练侧 `sampler` 生效
    - 不回写到 dataset build 结果

## 验证结果

- `PYTHONPATH=src python -m pytest -q --confcutdir=tests/training_repo tests/training_repo/test_dataset_build_min_loop.py`
  - 结果：`5 passed`
- `PYTHONPATH=src python -m pytest -q --confcutdir=tests/training_repo tests/training_repo/test_train_openpi_smoke.py`
  - 结果：`1 passed`
- `PYTHONPATH=src python -m pytest -q --confcutdir=tests/training_repo tests/training_repo/test_dataset_build_min_loop.py tests/training_repo/test_train_openpi_smoke.py`
  - 结果：`6 passed`
- 构建 + smoke（临时目录）：
  - `PYTHONPATH=src python scripts/build_dataset.py --config <tmp_config>`
  - `PYTHONPATH=src python scripts/smoke_dataset_openpi.py --dataset-root <tmp_dataset_root>`
  - 结果：`dataset_openpi_smoke passed`

补充说明：仓库根 `tests/conftest.py` 依赖 `torch`，为避免超出本任务范围，我使用 `--confcutdir=tests/training_repo` 只运行本次新增闭环测试，不引入额外大依赖。

