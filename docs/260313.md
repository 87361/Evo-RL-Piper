1. **验收目标**  
- 验证 `v0 export slice` 在可用环境下可稳定完成：  
  - 产出自包含 `inference_bundle`；  
  - `load_policy()` / `predict_action()` 可用；  
  - 关键错误场景（字段/shape/非法权重）报错清晰；  
  - 模拟 TeleManipulation 接入 smoke 通过。  

2. **前置条件检查清单**  
- Python 环境可用，且已安装 `numpy`、`PyYAML`、`pytest`。  
- 仓库根目录存在并可写：`configs/export_inference.yaml`。  
- `configs/export_inference.yaml` 中以下项正确：  
  - `artifact_dir` 指向训练产物目录；  
  - `artifact_weights_file` 指向实际权重文件名；  
  - `tos_weights_path` 为部署约定路径。  
- `${artifact_dir}/${artifact_weights_file}` 文件存在且可读。  
- 允许创建/覆盖 `deploy/inference_bundle` 与 `tmp/telemanipulation_smoke`。  

3. **必跑命令列表**  
- `python scripts/export_inference_bundle.py --config configs/export_inference.yaml`  
- `python -m pytest -q tests/training_repo/test_export_bundle.py`  
- `python scripts/smoke_telemanipulation_bundle.py --bundle-dir deploy/inference_bundle`  

4. **每条命令的预期结果**  
- `export_inference_bundle.py`  
  - 退出码 `0`；  
  - 打印 `output_dir/obs_dim/action_dim/artifact_weights_file/files`；  
  - `deploy/inference_bundle` 内含 8 个核心文件（`inference_runner.py`、`model_spec.yaml`、`weights_locator.yaml` 等）。  
- `pytest test_export_bundle.py`  
  - `1 passed`；  
  - 覆盖：配置驱动权重名、README 边界文案、正向推理、3 类负向报错。  
- `smoke_telemanipulation_bundle.py`  
  - 退出码 `0`；  
  - 打印 `telemanipulation bundle smoke passed`；  
  - 输出 checks 列表包含 4 项（正向 + 3 个错误场景）。  

5. **失败时优先排查项**  
- **环境问题优先**：`numpy/pytest/PyYAML` 缺失、解释器不一致。  
- **配置问题**：`artifact_weights_file` 与真实文件名不一致。  
- **路径问题**：`artifact_dir`、`--bundle-dir` 指向错误或无权限。  
- **权重内容问题**：npz 缺少 `weight/bias/obs_mean/obs_std/action_mean/action_std`。  
- **维度问题**：`obs_state_dim` 与输入向量长度不一致。  

6. **通过验收的判定标准**  
- 上述 3 条必跑命令全部成功（退出码均为 `0`）。  
- `deploy/inference_bundle` 文件集完整且可重复生成。  
- 单测与 smoke 均验证到：  
  - 正向路径可用；  
  - 负向错误信息可读、可定位。  
- 无“仅静态检查通过、未运行验证”的残留状态。  

7. **当前明确不在验收范围内**  
- 完整训练链路（`build_dataset` / `train_openpi`）是否收敛。  
- TeleManipulation 真仓联调与真实机器人执行效果。  
- 在线推理服务化、性能压测、并发稳定性。  
- 权重生产流程治理（版本管理、发布流水线、回滚策略）。

复验结果（三条必跑命令）：
python scripts/export_inference_bundle.py --config configs/export_inference.yaml
退出码 0，成功打印预期字段。
python -m pytest -q tests/training_repo/test_export_bundle.py
退出码 0，1 passed。
python scripts/smoke_telemanipulation_bundle.py --bundle-dir deploy/inference_bundle
退出码 0，输出 telemanipulation bundle smoke passed，checks 为 4 项（1 正向 + 3 错误场景）。
额外说明：
我这次补的是验收用最小 mock 权重。如果你要对接真实训练产物，只需要把 artifacts/openpi_v0/run_001/model.npz 替换成真实权重（或改 artifact_weights_file 指向真实文件）即可。



实现围绕 4 点收口：相对路径、episode 级 split、全局唯一 sample_id、OpenPI 最小可读契约显式化。

## 已完成改动

- **builder 稳健性收口**
  - 更新 `src/training_repo/dataset_build/builder.py`
  - 新增：
    - 配置路径解析：相对路径以配置文件目录为锚点解析
    - `split_mode` 仅支持 `episode`（默认）
    - `shard_size > 0` 校验
    - `sample_id` 格式强约束：`<episode_id>:<t>`
    - `sample_id` 全局唯一校验（build 全过程）
    - `obs_state/action` 维度一致性校验
- **配置对齐 v2**
  - 更新 `configs/dataset_build.yaml`
  - 显式加入 `split_mode: episode`
- **类型定义同步**
  - 更新 `src/training_repo/dataset_build/config.py`
  - `DatasetBuildConfig` 增加 `split_mode`
- **最小 fixture 数据**
  - 新增 `tests/fixtures/training_repo/raw_episodes/episode_0001.json`
  - 新增 `tests/fixtures/training_repo/raw_episodes/episode_0002.json`
- **E2E 最小闭环测试**
  - 新增 `tests/training_repo/test_dataset_build_min_loop.py`
  - 覆盖：
    - relabel 三类样本 + pre-intervention
    - episode 级 split
    - sample_id 全局唯一
    - OpenPI 最小契约字段
    - stats 维度一致性
    - adapter 可读 train/val
- **CLI smoke**
  - 新增 `scripts/smoke_dataset_openpi.py`
  - 校验数据包最小契约、跨文件 `sample_id` 对齐、`bucket==sample_type`、stats 维度、OpenPI adapter 可读性
- **契约文档显式化**
  - 新增 `docs/dataset-build-min-loop-v0.md`
  - 明确最小字段、路径规则、split/sample_id 规则与 smoke 命令

## 验证结果

- `PYTHONPATH=src python -m pytest -q --confcutdir=tests/training_repo tests/training_repo/test_dataset_build_min_loop.py`
  - 结果：`5 passed`
- 构建 + smoke（临时目录）：
  - `PYTHONPATH=src python scripts/build_dataset.py --config <tmp_config>`
  - `PYTHONPATH=src python scripts/smoke_dataset_openpi.py --dataset-root <tmp_dataset_root>`
  - 结果：`dataset_openpi_smoke passed`

补充说明：仓库根 `tests/conftest.py` 依赖 `torch`，为避免超出本任务范围，我使用 `--confcutdir=tests/training_repo` 只运行本次新增闭环测试，不引入额外大依赖。
